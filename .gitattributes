# Finales de línea forzados a LF para lo que corre en el servidor Linux.
# Un script .sh o un ruleset de nftables con CRLF de Windows NO arranca en Linux
# (bash: "bad interpreter"), y este repo se edita también desde Windows/Laragon.
*.sh              text eol=lf
deploy/*.conf     text eol=lf
